# mosquitto.conf
listener 1883
allow_anonymous true

# Log everything useful
log_type all
log_timestamp true

# Optional: also log to file (keeps history in container filesystem)
# log_dest file /mosquitto/log/mosquitto.log
log_dest stdout
